{% from "_components/blog/layouts/listing" import ListingLayout %}

{% set entries = craft.entries()
    .section('blog')
    .relatedTo([entry.id])
    .orderBy('postDate ASC')
    .all()
%}

{{ ListingLayout(
    entries: entries,
    parentEntry: entry
) }}